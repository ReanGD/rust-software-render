language: rust
sudo: required
rust:
  - beta
  - nightly
  - stable
os:
  - linux
install:
  # wget http://www.libsdl.org/tmp/SDL-2.0.4-9799.tar.gz
  # tar -xzvf SDL-2.0.4-9799.tar.gz
  # cd SDL-2.0.4-9799 && ./configure
  - sudo add-apt-repository ppa:team-xbmc/ppa -y
  - sudo apt-get update -q
  - sudo apt-get install libsdl2-dev
before_script:
  - rustc -V
  - cargo -V
script:
  - cargo build --release -v
  - cargo test -v
notifications:
  email:
    recipients:
      - reangd@yandex.ru
    on_success: never
    on_failure: always